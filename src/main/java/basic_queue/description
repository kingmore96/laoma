设计文档：实现一个消息队列，持久化消息，并用于不同程序间共享消息。
1、接口
    1）构造方法
    2）入队
    3）出队
2、设计细节
    1）持久化消息，使用.data保存数据文件，使用.meta保存元数据文件。
    2）共享消息，使用内存映射文件机制，通过文件的getChannel得到FileChannel，根据channel映射文件和
       字节数组。
    3）.data文件中使用固定长度存储消息，定长1024，前4个字节为消息实际长度，每条消息最大长度不超过1020
       设计队列中可以保存的最大消息数量为1024*1024，每条消息长度1024，总长度为1024*1024*1024
    4）.meta文件中保存数组的头和尾索引，各四字节。初始均为0，入队增加尾，出队增加头，若到结尾，再从
       0开始，模拟循环队列。
    5）为区分队列满与空，规定始终保留一个位置不存数据。头与尾相等为空，（队列尾+1个消息长度）与长度
       取模等于队列头为满。
    6）暂不考虑并发引起的一致性问题。

